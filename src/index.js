import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';

// Placeholder: App.js was deleted in v2 rebuild Commit 4.
// Replace with the real v2 App component once the 5-screen router is built.
function App() {
  return <div style={{ padding: 32, fontFamily: 'system-ui' }}>Simplifii-OS v2 shell. Rebuild in progress.</div>;
}

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<React.StrictMode><App /></React.StrictMode>);
